Pruebas
Backend

Unit: funciones puras (parseo/validación/cálculo KPI).

Integración: rutas con supertest (import, alerts, packages).

E2E smoke (opcional): flujo import→alerta→cambio de estado→timeline.

Frontend

Unit/Component: React Testing Library para componentes clave.

E2E (opcional): Playwright/Cypress para un smoke básico (login→importar→ver alerta).

Siempre incluir cómo correr las pruebas (npm test, npm run test:e2e).
---
alwaysApply: true
---
